/**
 * Данные экрана «Вопросы»: строки статуса проекта и ссылки подвала.
 * Сами вопросы и ответы — в src/content/faq.ts: из них же собирается
 * разметка FAQPage для поисковиков, поэтому здесь они не дублируются.
 */
import { SECTIONS, type SectionInfo } from '@/content/sections'

/** Строка статуса: подпись слева, значение моноширинным шрифтом справа. */
export interface StatusRow {
  label: string
  value: string
}

/**
 * Вместо отзывов и логотипов, которых у проекта нет, — честный статус:
 * что сделано, а что нет. Это работает как элемент доверия и заодно
 * снимает вопрос «а это уже продаётся?».
 */
export const STATUS_ROWS: readonly StatusRow[] = [
  { label: 'Стадия проекта', value: 'готовим прототип к релизу весной 2027' },
  { label: 'Как купить', value: 'старт предзаказов ожидаем к концу 2026' },
  { label: 'Как стать частью проекта', value: 'оставьте заявку на альфа-версию' },
]

/**
 * Колонка «Разделы» в подвале: все экраны по порядку, кроме последнего —
 * подвал стоит на нём самом. Берётся из общего списка экранов, поэтому
 * новый экран появится в подвале без правки этого файла.
 */
export const FOOTER_NAV: readonly SectionInfo[] = SECTIONS.slice(0, -1)
