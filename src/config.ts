/**
 * Настройки формы заявки и контакты команды.
 * Почту и состав команды меняют только здесь: они же подставляются в текст
 * согласия (RequestConsent.vue) и в подвал (SiteFooter.vue).
 * Заявка уходит письмом на почту команды через FormSubmit: сайт статический,
 * своего сервера у него нет. Код отправки — src/services/requestForm.ts.
 */

/** Почта команды: сюда приходят заявки, она же — в запасном письме (mailto). */
export const TEAM_EMAIL = 'Evtsik@yandex.ru'

/** Состав команды — оператор персональных данных в тексте согласия и строка в подвале. */
export const TEAM_MEMBERS = 'Жученко В.А., Евцихевич М.А., Пыжов Д.И., Нугумаев А.А.'

/** Тема письма с заявкой (и в FormSubmit, и в запасном письме). */
export const REQUEST_SUBJECT = 'Заявка на тест прототипа WEXUS'

/* ДО ЗАПУСКА: один раз подтвердите адрес. Отправьте тестовую заявку —
   FormSubmit пришлёт на TEAM_EMAIL письмо «Activate Form» (проверьте и
   папку «Спам», отметьте письмо как «Не спам»), нажмите кнопку в нём.
   Заявки до подтверждения сервис хранит 30 дней и доставит после него.
   Вместе с подтверждением приходит случайная строка — её можно подставить
   в SEND_URL вместо почты, чтобы адрес не светился в коде страницы. */
export const SEND_URL = 'https://formsubmit.co/ajax/' + TEAM_EMAIL

/** Сколько ждать ответа сервиса, мс. Дольше — считаем, что заявка не ушла. */
export const SEND_TIMEOUT_MS = 15000

/* Со страниц, открытых как файл (file://), FormSubmit заявки не принимает.
   Чтобы проверить форму с диска, впишите сюда адрес сайта
   (например, 'https://wexus.ru/') — по справке сервиса он подставит его
   вместо адреса страницы. На сайте поле можно оставить пустым. */
export const SITE_URL = ''
